<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/includedEyeListTitle"
        style="@style/basicMediumTextView"
        android:textSize="16dp"
        android:text="제품추가"
        android:textColor="@color/main_black"
        app:layout_constraintTop_toTopOf="@+id/includedEyeListCancel"
        app:layout_constraintBottom_toBottomOf="@+id/includedEyeListCancel"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <ImageView
        android:id="@+id/includedEyeListCancel"
        android:layout_width="26dp"
        android:layout_height="26dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="16dp"
        app:tint="@color/main_black"
        android:src="@drawable/cancel" />


</androidx.constraintlayout.widget.ConstraintLayout>