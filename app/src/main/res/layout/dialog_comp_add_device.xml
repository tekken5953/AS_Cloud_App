<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/compAddTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        android:text="@string/complete_register"
        android:textStyle="bold"
        android:textSize="24sp"
        android:gravity="center"
        android:textColor="@color/main_gray_color"
        android:layout_marginTop="120dp"
        app:drawableTopCompat="@drawable/check"
        android:drawablePadding="4dp"/>

    <FrameLayout
        android:id="@+id/compAddFrame"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="50dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginTop="50dp"
        app:layout_constraintBottom_toTopOf="@+id/compAddOkBtn"
        app:layout_constraintTop_toBottomOf="@+id/compAddTitle"
        android:background="@drawable/add_device_field_bg">

        <ImageView
            android:id="@+id/compAddFrameImage"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:layout_gravity="center"
            android:src="@drawable/sunny"/>
    </FrameLayout>

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/compAddOkBtn"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginTop="50dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/compAddFrame"
        android:background="@drawable/add_device_button_bg"
        android:text="@string/complete"
        android:textColor="@color/mode_color_view"
        android:textSize="20sp"/>

</androidx.constraintlayout.widget.ConstraintLayout>